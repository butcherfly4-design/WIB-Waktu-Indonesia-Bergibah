"use client";
import { useState } from "react";

export default function Login(){
 const [error,setError]=useState("");
 const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setError("");const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({login:f.get("login"),password:f.get("password")})});const d=await r.json();if(!r.ok)return setError(d.error||"Login gagal");location.href="/"};
 return <main className="grid min-h-[calc(100vh-64px)] place-items-center px-4"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-7"><div className="text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-gradient-to-br from-purple-500 to-pink-500 font-black">WIB</div><h1 className="mt-5 text-3xl font-black">Balik lagi? 👀</h1><p className="mt-2 text-sm text-zinc-500">Gibah belum selesai.</p></div><div className="mt-7 space-y-3"><input name="login" required placeholder="Email atau username" className="w-full rounded-2xl border border-white/10 bg-black/20 px-4 py-3"/><input name="password" type="password" required placeholder="Password" className="w-full rounded-2xl border border-white/10 bg-black/20 px-4 py-3"/>{error&&<p className="text-sm text-red-300">{error}</p>}<button className="w-full rounded-2xl bg-white px-4 py-3 font-bold text-black">Login</button></div></form></main>
}