"use client";
import { useState } from "react";
export default function Register(){const [error,setError]=useState("");const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setError("");const f=new FormData(e.currentTarget);if(f.get("password")!==f.get("confirm"))return setError("Konfirmasi password tidak sama.");const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:f.get("username"),email:f.get("email"),password:f.get("password")})});const d=await r.json();if(!r.ok)return setError(d.error||"Pendaftaran gagal");location.href="/"};return <main className="grid min-h-[calc(100vh-64px)] place-items-center px-4"><form onSubmit={submit} className="glass w-full max-w-md rounded-3xl p-7"><div className="text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-gradient-to-br from-purple-500 to-pink-500 font-black">WIB</div><h1 className="mt-5 text-3xl font-black">Gabung ke WIB 🗣️</h1><p className="mt-2 text-sm text-zinc-500">Bikin akun. Lalu pura-pura cuma lewat.</p></div><div className="mt-7 space-y-3"><input name="username" required placeholder="Username" className="w-full rounded-2xl border border-white/10 bg-black/20 px-4 py-3"/><input name="email" required type="email" placeholder="Email" className="w-full rounded-2xl border border-white/10 bg-black/20 px-4 py-3"/><input name="password" required minLength={8} type="password" placeholder="Password (min. 8 karakter)" className="w-full rounded-2xl border border-white/10 bg-black/20 px-4 py-3"/><input name="confirm" required type="password" placeholder="Konfirmasi password" className="w-full rounded-2xl border border-white/10 bg-black/20 px-4 py-3"/>{error&&<p className="text-sm text-red-300">{error}</p>}<button className="w-full rounded-2xl bg-gradient-to-r from-purple-500 to-pink-500 px-4 py-3 font-bold">Daftar & Mulai Gibah</button></div></form></main>}